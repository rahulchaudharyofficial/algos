#ifndef _UTILS_H_
#define _UTILS_H_
    #include<stdbool.h>
    bool array_eq(const int* a1, const int* a2);
    int priority(char);
    bool is_operator(char);
    int calculate(int, int, int);
#endif